<main class="page-layout">
  {{{Navigation}}}
  <div class="card card_el-1 card-signin">
    <div class="card__header">
      <h3>Вход</h3>
    </div>
    <div class="card__body">
      {{#if isError}}
        {{{Error}}}
      {{/if}}
      {{{Form}}}
    </div>
  </div>
</main>
